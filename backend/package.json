{
  "name": "doohgle-backend",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "migrate": "node -e \"require('dotenv').config(); const { exec } = require('child_process'); exec(`psql ${process.env.DATABASE_URL} -f migrations/001_initial_schema.sql`, (err, stdout, stderr) => { if (err) { console.error(err); return; } console.log(stdout); console.error(stderr); });\"",
    "migrate:otp": "node -e \"require('dotenv').config(); const { exec } = require('child_process'); exec(`psql ${process.env.DATABASE_URL} -f migrations/003_add_otp_table.sql`, (err, stdout, stderr) => { if (err) { console.error(err); return; } console.log(stdout); console.error(stderr); });\""
  },
  "dependencies": {
    "@types/axios": "^0.9.36",
    "@types/nodemailer": "^6.4.17",
    "axios": "^1.11.0",
    "bcrypt": "^5.1.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^7.0.5",
    "pg": "^8.11.1"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.2",
    "@types/node": "^20.19.10",
    "@types/pg": "^8.15.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
